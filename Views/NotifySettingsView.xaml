<UserControl x:Class="SlappyHub.Views.NotifySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <!-- Form -->
    <Grid>
    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="スロットで通知するメッセージ" FontWeight="SemiBold" Margin="0,16,0,6" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="8"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <RadioButton Grid.Row="0" Grid.Column="0" Content="チャンネル" GroupName="TargetType" Margin="16 0 0 0"
                             IsChecked="{Binding IsDirectMessage, Converter={StaticResource InvertBoolConverter}, Mode=TwoWay}" />
                <RadioButton Grid.Row="2" Grid.Column="0" Content="DM" GroupName="TargetType" Margin="16 0 0 0"
                             IsChecked="{Binding IsDirectMessage, Mode=TwoWay}" />

                <!-- Channel -->
                <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"
                            Visibility="{Binding IsDirectMessage, Converter={StaticResource InvertBoolToVisibilityConverter}}">
                    <TextBlock Text="チャンネル名（例：general / random / proj-xxx）" Opacity="0.7" />
                    <TextBox Text="{Binding ChannelName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,6,0,0" />
                </StackPanel>

                <!-- DM -->
                <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"
                            Visibility="{Binding IsDirectMessage, Converter={StaticResource BoolToVisibilityConverterEx}}">
                    <TextBlock Text="ダイレクトメッセージを通知します" Opacity="0.7" />
                </StackPanel>
            </Grid>

            <!-- LED pattern -->
            <Grid Margin="0,16,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="LED 発光パターン（LedPattern）" FontWeight="SemiBold" />
                <!-- 本体ComboBox：ドロップダウン表示を担当 -->
                <ComboBox Grid.Row="0" Grid.Column="1" x:Name="LedPatternCombo" HorizontalAlignment="Left"
                          ItemsSource="{Binding LedPatterns}"
                          SelectedItem="{Binding SelectedLedPattern}"
                          Height="28" Width="500">

                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <Border Height="12"
                                    Margin="4,2"
                                    CornerRadius="6"
                                    BorderThickness="1"
                                    BorderBrush="#33000000"
                                    Background="{Binding PreviewBrush}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>

                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>

                <ContentControl Grid.Row="0" Grid.Column="1" IsHitTestVisible="False" Width="450"
                                Margin="6,0,30,0"
                                VerticalAlignment="Center" HorizontalAlignment="Left"
                                Content="{Binding ElementName=LedPatternCombo, Path=SelectedItem}"
                                ContentTemplate="{Binding ElementName=LedPatternCombo, Path=ItemTemplate}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <!-- ドロップダウンを開いている間は重ね表示を消す -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=LedPatternCombo, Path=IsDropDownOpen}"
                                             Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
                <Button Grid.Row="0" Grid.Column="2" Content="▶️" BorderThickness="0"
                        Background="Transparent" FontSize="14pt" FontFamily="Segoe UI Emoji"
                        VerticalAlignment="Center" Command="{Binding TestLedPatternCommand}"
                        Foreground="#0078D4"
                        ToolTip="LEDのテスト発光">
                </Button>
                <Button Grid.Row="0" Grid.Column="3" Content="⏹️" BorderThickness="0"
                        Background="Transparent" FontSize="14pt" FontFamily="Segoe UI Emoji"
                        VerticalAlignment="Center" Command="{Binding StopLedPatternCommand}"
                        Foreground="#cc0000"
                        ToolTip="LEDのテスト停止">
                </Button>

            </Grid>

            <!-- Sound -->
            <Grid Margin="0,16,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="着信音" FontWeight="SemiBold" />
                <Grid Grid.Row="0" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0"  ItemsSource="{Binding AvailableSounds}"
                               Margin="0 0 8 8"
                               SelectedItem="{Binding SelectedSound, Mode=TwoWay}"
                               DisplayMemberPath="DisplayName" />
                    <Button Grid.Column="1" Content="▶️" BorderThickness="0"
                            Background="Transparent" FontSize="14pt" FontFamily="Segoe UI Emoji"
                            VerticalAlignment="Top" Command="{Binding PlayDeviceSoundCommand}"
                            IsEnabled="{Binding CanPlayDeviceSound}"
                            ToolTip="選択中のファイルをSlappyBellでテスト再生"
                            Visibility="{Binding IsDeviceSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="#FF9E9E9E"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanPlayDeviceSound}" Value="True">
                                        <Setter Property="Foreground" Value="#0078D4"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </DataTrigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Foreground" Value="#FF9E9E9E"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Grid.Column="2" Content="🗑️" BorderThickness="0"
                            Background="Transparent" FontSize="14pt" FontFamily="Segoe UI Emoji"
                            VerticalAlignment="Top" Command="{Binding DeleteDeviceSoundFileCommand}"
                            IsEnabled="{Binding CanDeleteDeviceSoundFile}"
                            ToolTip="選択中のファイルをSlappyBellから削除"
                            Visibility="{Binding IsDeviceSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}"/>

                </Grid>
                <!-- Sound File upload -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="アップロードするファイル" Opacity="0.7" HorizontalAlignment="Right"
                           Margin="0 8 14 0"
                           Visibility="{Binding IsUploadSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}" />
                <Grid Grid.Row="1" Grid.Column="1" Margin="0 8 0 0"
                      Visibility="{Binding IsUploadSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="40" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Row="0" Grid.Column="0" Margin="0 0 4 0"
                             Text="{Binding UploadSoundFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Row="0" Grid.Column="1" Margin="0 0 4 0" Content="..."
                            Command="{Binding BrowseUploadSoundFileCommand}" />
                    <Button Grid.Row="0" Grid.Column="2" Content="▶️" BorderThickness="0"
                            Background="Transparent" FontSize="14pt" FontFamily="Segoe UI Emoji"
                            VerticalAlignment="Center" Command="{Binding PlayUploadSoundFileCommand}"
                            IsEnabled="{Binding CanPlayUploadSoundFile}"
                            ToolTip="テスト再生">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="#FF9E9E9E"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanPlayUploadSoundFile}" Value="True">
                                        <Setter Property="Foreground" Value="#0078D4"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </DataTrigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Foreground" Value="#FF9E9E9E"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="SlappyBell内のファイル名" Opacity="0.7"
                           HorizontalAlignment="Right" Margin="0 8 14 0"
                           Visibility="{Binding IsUploadSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}" />
                <Grid Grid.Row="2" Grid.Column="1" Margin="0 8 0 0"
                      Visibility="{Binding IsUploadSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="40" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Row="0" Grid.Column="0" Margin="0 0 4 0"
                             Text="{Binding UploadDeviceSoundFile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Row="0" Grid.Column="1" Content="⬆️" BorderThickness="0"
                            Background="Transparent" FontSize="14pt" FontFamily="Segoe UI Emoji"
                            Command="{Binding UploadSoundFileCommand}"
                            ToolTip="選択したMP3ファイルをSlappyBellにアップロードします"/>
                </Grid>
                <!--
                <Popup IsOpen="{Binding IsUploadPopupOpen}"
                       Placement="Center"
                       AllowsTransparency="True"
                       StaysOpen="False">
                    <Border Background="#CC000000"
                            CornerRadius="12"
                            Padding="16">
                        <StackPanel Width="320">
                            <TextBlock Text="アップロード"
                                       Foreground="White"
                                       FontSize="14"
                                       Margin="0,0,0,8"/>
                            <ProgressBar Minimum="0"
                                         Maximum="100"
                                         Height="16"
                                         Value="{Binding UploadPercent}"/>
                            <TextBlock Text="{Binding UploadStatus}"
                                       Foreground="White"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </Popup>
                -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="URL（Wi-Fi有効時）" Opacity="0.7" HorizontalAlignment="Right"
                           Visibility="{Binding IsUrlSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}" />
                <Grid Grid.Row="1" Grid.Column="1"
                       Visibility="{Binding IsUrlSoundSelected, Converter={StaticResource BoolToVisibilityConverterEx}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="40" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Row="0" Grid.Column="0"
                             Text="{Binding SoundUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Grid.Row="0" Grid.Column="1" Content="▶️" BorderThickness="0"
                            Background="Transparent" FontSize="14pt" FontFamily="Segoe UI Emoji"
                            VerticalAlignment="Center" Command="{Binding PlaySoundUrlCommand}"
                            IsEnabled="{Binding CanPlaySoundUrl}"
                            ToolTip="テスト再生 (Wi-Fi接続が必要です)">
                        <Button.Style>
                            <Style TargetType="Button">
                                <!-- 無効時（デフォルト） -->
                                <Setter Property="Foreground" Value="#FF9E9E9E"/>
                                <Setter Property="Cursor" Value="Arrow"/>

                                <Style.Triggers>
                                    <!-- 有効時 -->
                                    <DataTrigger Binding="{Binding CanPlaySoundUrl}" Value="True">
                                        <Setter Property="Foreground" Value="#0078D4"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </DataTrigger>

                                    <!-- IsEnabled=False でも念のため -->
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Foreground" Value="#FF9E9E9E"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>

            <!-- SenderFilter tags -->
            <TextBlock Text="通知しない送信者" FontWeight="SemiBold" Margin="0 16 0 0" />
            <Border BorderBrush="#FFE0E0E0"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="8"
                    Background="White">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <WrapPanel>

                        <ItemsControl ItemsSource="{Binding SenderFilterTokens}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FF3A3A3A"
                                            CornerRadius="8"
                                            Margin="0,0,8,8"
                                            Padding="10,6">
                                        <DockPanel LastChildFill="False">
                                            <TextBlock Text="{Binding}" Foreground="White"
                                                       VerticalAlignment="Center" />
                                            <Button Content="×"
                                                    DockPanel.Dock="Right"
                                                    Margin="8,0,0,0"
                                                    Padding="6,0"
                                                    Background="Transparent"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.RemoveSenderTokenCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" />
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBox Width="250"
                                 MinWidth="140"
                                 Margin="0,0,8,8"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Text="{Binding NewSenderTokenText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Enterで追加"
                                 KeyDown="SenderTokenInput_KeyDown" />
                    </WrapPanel>
                </ScrollViewer>
            </Border>

            <!-- TextFilter tags -->
            <TextBlock Text="通知しない本文" FontWeight="SemiBold" Margin="0,16,0,0" />
            <Border BorderBrush="#FFE0E0E0"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="8"
                    Background="White">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <WrapPanel>

                        <ItemsControl ItemsSource="{Binding TextFilterTokens}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FF3A3A3A"
                                            CornerRadius="8"
                                            Margin="0,0,8,8"
                                            Padding="10,6">
                                        <DockPanel LastChildFill="False">
                                            <TextBlock Text="{Binding}" Foreground="White"
                                                       VerticalAlignment="Center" />
                                            <Button Content="×"
                                                    DockPanel.Dock="Right"
                                                    Margin="8,0,0,0"
                                                    Padding="6,0"
                                                    Background="Transparent"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.RemoveTextTokenCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" />
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBox MinWidth="250"
                                 Margin="0,0,8,8"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Text="{Binding NewTextTokenText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Enterで追加"
                                 KeyDown="TextTokenInput_KeyDown" />
                    </WrapPanel>
                </ScrollViewer>
            </Border>
            <!-- Actions -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 8 0 0">
                <Button Content="キャンセル" Width="80" Padding="4,4" Margin="0,0,14,0"
                        Command="{Binding CancelCommand}" />
                <Button Content="OK" Width="80" Padding="4,4"
                        IsDefault="True"
                        Command="{Binding OkCommand}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
        <!-- Upload Overlay -->
        <Grid Panel.ZIndex="9999"
              Background="Transparent"
              Visibility="{Binding IsUploading, Converter={StaticResource BoolToVisibilityConverterEx}}"
              IsHitTestVisible="True"
              Focusable="True">
            <Border Width="360"
                    Padding="16"
                    CornerRadius="12"
                    Background="#CC000000"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="アップロード"
                               Foreground="White"
                               FontSize="14"
                               Margin="0,0,0,8"/>
                    <ProgressBar Minimum="0"
                                 Maximum="100"
                                 Height="16"
                                 Value="{Binding UploadPercent}"/>
                    <TextBlock Text="{Binding UploadStatus}"
                               Foreground="White"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>